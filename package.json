{
  "name": "cert-management",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node dist/cert-maintenance.js",
    "start:dev": "ts-node src/cert-maintenance.ts",
    "local-prepare": "npm run lsmcp:init && npm run lsmcp:index && npm run playwright:install",
    "build": "tsc",
    "playwright:install": "npx playwright install --with-deps chromium",
    "lsmcp:init": "npx @mizchi/lsmcp init -p typescript",
    "lsmcp:index": "npx @mizchi/lsmcp index -p typescript",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint \"{src,apps,libs,test}/**/*.ts\"",
    "lint:fix": "eslint \"{src,apps,libs,test}/**/*.ts\" --fix"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.19.1",
    "@typescript-eslint/parser": "^6.19.1",
    "eslint": "^8.56.0",
    "eslint-plugin-import": "^2.29.1",
    "eslint-plugin-simple-import-sort": "^10.0.0",
    "eslint-plugin-unused-imports": "^3.0.0",
    "jest": "^30.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "form-data": "^4.0.4",
    "playwright": "^1.55.0"
  }
}
